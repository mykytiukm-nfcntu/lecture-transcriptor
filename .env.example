# This repository does NOT use a shared root .env.
#
# Each project owns its own environment file:
#   backend/.env   — copy from backend/.env.example (all runtime settings live here)
#   frontend/.env  — added in a later step; the Vite dev server needs none today
#
# Do not add variables to this file. It exists only as a signpost.
